# Chatterbox TTS Server Dependencies
chatterbox-tts>=0.1.6
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
numpy>=1.24.0,<1.26.0
soundfile>=0.12.0

# MLX-based TTS (Kokoro - Fast tier)
# NOTE: mlx-audio requires numpy>=1.26.4 which conflicts with chatterbox-tts
# Fast tier is currently unavailable due to this dependency conflict
# mlx>=0.21.0
# mlx-audio>=0.1.0
# sounddevice>=0.5.0
